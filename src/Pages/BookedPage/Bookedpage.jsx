import { useParams } from "react-router-dom";


const Bookedpage = () => {
    const {params}=useParams();
    console.log(params);
    return (
        <div>
            
        </div>
    );
};

export default Bookedpage;